#ifndef __SYNC_H__
#define __SYNC_H__

#include <stdio.h>
//#include <fstream>
#include <stdlib.h>
#include <string.h>


int is_terminated();
void term_prog(int loop, char * prog);
void listen(int loop, char * prog);

#endif

